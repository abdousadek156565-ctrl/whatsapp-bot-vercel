# Google App Engine configuration
runtime: nodejs18

env_variables:
  NODE_ENV: production
  PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: true

automatic_scaling:
  min_instances: 0
  max_instances: 1
  target_cpu_utilization: 0.6

resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10